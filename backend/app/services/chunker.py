"""Document chunker service.

Splits extracted text into semantically meaningful chunks for embedding
and retrieval. Supports configurable chunk sizes, overlap, and
vehicle-aware chunking strategies.
"""
