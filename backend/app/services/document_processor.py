"""Document processor service.

Handles ingestion and processing of uploaded documents (PDFs, images, CSVs).
Extracts text content, coordinates OCR when needed, and triggers the
chunking and embedding pipeline.
"""
